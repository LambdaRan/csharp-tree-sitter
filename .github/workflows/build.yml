name: build

on:
  push:
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version (e.g. 0.1.0), leave empty for CI build'
        required: false

jobs:
  # ── Native library builds (parallel) ──────────────────────────

  build-native-linux-x64:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
      - name: Build tree-sitter for linux-x64
        run: NativeLibs/build/tree-sitter/make-linux.sh linux-x64
      - uses: actions/upload-artifact@v4
        with:
          name: native-linux-x64
          path: NativeLibs/out/

  build-native-macos-arm64:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
      - name: Build tree-sitter for osx-arm64
        run: NativeLibs/build/tree-sitter/make-macos.sh osx-arm64
      - uses: actions/upload-artifact@v4
        with:
          name: native-macos-arm64
          path: NativeLibs/out/

  build-native-windows-x64:
    runs-on: windows-2022
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
      - name: Build tree-sitter for win-x64
        run: NativeLibs\build\tree-sitter\make-windows.bat win-x64
      - uses: actions/upload-artifact@v4
        with:
          name: native-windows-x64
          path: NativeLibs/out/

  # ── Pack NuGet packages ──────────────────────────────────────

  build-and-pack:
    runs-on: ubuntu-latest
    needs:
      - build-native-linux-x64
      - build-native-macos-arm64
      - build-native-windows-x64
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: |
            8.0.x
            10.0.x

      - name: Download all native artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: native-*
          merge-multiple: true
          path: NativeLibs/out

      - name: Pack TSBind
        run: |
          if [ -n "${{ inputs.version }}" ]; then
            dotnet pack TSBind/TSBind.csproj -c Release -p:Version=${{ inputs.version }}
          else
            dotnet pack TSBind/TSBind.csproj -c Release -p:VersionSuffix=ci.${{ github.run_number }}
          fi

      - name: Pack TSWrap
        run: |
          if [ -n "${{ inputs.version }}" ]; then
            dotnet pack TSWrap/TSWrap.csproj -c Release -p:Version=${{ inputs.version }}
          else
            dotnet pack TSWrap/TSWrap.csproj -c Release -p:VersionSuffix=ci.${{ github.run_number }}
          fi

      - name: Upload NuGet packages
        uses: actions/upload-artifact@v4
        with:
          name: nuget-packages
          path: |
            TSBind/bin/Release/*.nupkg
            TSWrap/bin/Release/*.nupkg
